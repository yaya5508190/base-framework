spring:
  application:
    name: plugin-loader
plugin:
  path: C:\develop\x-project\spring-plugin-loader\plugin-demo\target
  scan: true
  context-path: /loader
logging:
  level:
    root: debug
server:
  servlet:
    context-path: /loader